// flex-wrap: Allows the flex items to wrap onto multiple lines if needed.
// Default behavior: In a flex container, items will try to fit in a single row (or column).
// flex-nowrap: Prevents wrapping. All flex items will be forced into a single line (row or column), even if they overflow.
// flex-wrap-reverse: The items will wrap onto new lines, but the new lines will appear in reverse order.

// When to Use antialiased
//    1. For better font clarity on macOS and iOS.
//    2. When using custom fonts to ensure smoother rendering.
//    3. For a lighter font appearance, making text more readable.

// default direction for flex is row.

//capitalize: Capitalizes the first letter of each word in the text.(uppercase,normal-case)
// text-base ensures the text is a normal readable size.

// text-justify: Justifies the text, meaning the text will be spaced out so that both the left and right edges of the text block,
//  align with the container's edges. This is commonly used for paragraphs or blocks of text to give a clean, uniform look.

// w-fit: Makes the width of the button fit its content. The width will adjust to the size of the text or any other content inside the button.

// max-md:w-full: On medium screen sizes and below (as defined in your Tailwind breakpoints), this sets the button’s width to 100%,
//  making it full-width. This is useful for responsive design on smaller devices.

// !important: This makes sure that the styles applied are not overridden by other conflicting styles. It forces the styles to take priority.

// line-clamp-1 → Limits the text to a single line and truncates it with an ellipsis (...) if it overflows.

// line-clamp-2 → Restricts the text to a maximum of two lines, adding an ellipsis if it overflows.
